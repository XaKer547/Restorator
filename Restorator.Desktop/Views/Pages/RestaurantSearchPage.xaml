<Page x:Class="Restorator.Desktop.Views.Pages.RestaurantSearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewmodels="clr-namespace:Restorator.Desktop.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewmodels:RestaurantSearchViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <DockPanel>
            <ui:AutoSuggestBox PlaceholderText="Введите название ресторана" ItemsSource="{Binding RestaurantNames}"/>

        </DockPanel>

        <ListBox ItemsSource="{Binding RestaurantsPreview}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <ui:TextBlock Text="{Binding Name}" FontTypography="Body" />
                        <ui:TextBlock Text="{Binding Description}" FontTypography="Subtitle" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding SearchRestaurantsCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
</Page>
